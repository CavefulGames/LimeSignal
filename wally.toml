[package]
name = "caveful-games/bindablelemon"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
licence = "MIT"
authors = ["jiwonz"]
description = "A fork of LemonSignal but based on roblox's BindableEvent"
realm = "shared"
exclude = ["**"]
include = [
    "default.project.json",
    "src",
    "src/**",
    "LICENSE",
    "wally.toml",
]

[dependencies]
threadpool = "caveful-games/threadpool@^0.1.0"
