[package]
name = "caveful-games/limesignal"
version = "1.10.0"
registry = "https://github.com/UpliftGames/wally-index"
licence = "MIT"
authors = ["Aspecky", "Sona"]
description = "A fork of LemonSignal with some unneeded features removed and the BindableEvent feature added."
realm = "shared"
exclude = ["**"]
include = [
    "default.project.json",
    "src",
    "src/**",
    "LICENSE",
    "wally.toml",
]

[dependencies]
